---
import merge from 'lodash.merge';
import { AstroSeo } from '@astrolib/seo';

import type { Props as AstroSeoProps } from '@astrolib/seo';

import { SITE, METADATA, I18N } from 'astrowind:config';
import type { MetaData } from '~/types';
import { getCanonical } from '~/utils/permalinks';

import { adaptOpenGraphImages } from '~/utils/images';

export interface Props extends MetaData {
  dontUseTitleTemplate?: boolean;
}

const {
  title,
  ignoreTitleTemplate = false,
  canonical = String(getCanonical(String(Astro.url.pathname))),
  robots = {},
  description,
  openGraph = {},
  twitter = {},
} = Astro.props;

// Default Twitter/OpenGraph image - use Astro's processed icon
const defaultImage = getCanonical('/_astro/icon.webp');

// Helper function to adapt Twitter images (similar to OpenGraph)
async function adaptTwitterImages(twitterOpenGraph: typeof openGraph = {}, site?: URL) {
  if (!site) return twitterOpenGraph;
  if (!twitterOpenGraph?.images?.length) return twitterOpenGraph;

  const images = twitterOpenGraph.images.map((image) => {
    if (typeof image === 'string') {
      try {
        return new URL(image, site).toString();
      } catch {
        return image;
      }
    }
    if (typeof image.url === 'string') {
      try {
        return { ...image, url: new URL(image.url, site).toString() };
      } catch {
        return image;
      }
    }
    return image;
  });

  return { ...twitterOpenGraph, images };
}

const seoProps: AstroSeoProps = merge(
  {
    title: '',
    titleTemplate: '%s',
    canonical: canonical,
    noindex: true,
    nofollow: true,
    description: undefined,
    openGraph: {
      url: canonical,
      site_name: SITE?.name,
      images: [{ url: defaultImage, width: 1200, height: 630 }],
      locale: I18N?.language || 'en',
      type: 'website',
    },
    twitter: {
      cardType: 'summary_large_image',
      handle: SITE?.metadata?.twitter?.handle || '@localai67177',
      site: SITE?.metadata?.twitter?.site || '@localai67177',
    },
  },
  {
    title: METADATA?.title?.default,
    titleTemplate: METADATA?.title?.template,
    noindex: typeof METADATA?.robots?.index !== 'undefined' ? !METADATA.robots.index : undefined,
    nofollow: typeof METADATA?.robots?.follow !== 'undefined' ? !METADATA.robots.follow : undefined,
    description: METADATA?.description,
    openGraph: METADATA?.openGraph,
    twitter: {
      ...METADATA?.twitter,
      cardType: METADATA?.twitter?.cardType || 'summary_large_image',
    },
  },
  {
    title: title,
    titleTemplate: ignoreTitleTemplate ? '%s' : undefined,
    canonical: canonical,
    noindex: typeof robots?.index !== 'undefined' ? !robots.index : undefined,
    nofollow: typeof robots?.follow !== 'undefined' ? !robots.follow : undefined,
    description: description,
    openGraph: {
      url: canonical,
      ...openGraph
    },
    twitter: {
      ...twitter,
      cardType: twitter?.cardType || 'summary_large_image',
      handle: twitter?.handle || SITE?.metadata?.twitter?.handle || '@localai67177',
      site: twitter?.site || SITE?.metadata?.twitter?.site || '@localai67177',
      image: twitter?.image || openGraph?.images?.[0]?.url || defaultImage,
    },
  }
);
---

<AstroSeo {...{ ...seoProps, openGraph: await adaptOpenGraphImages(seoProps?.openGraph, Astro.site), twitter: await adaptTwitterImages(seoProps?.twitter, Astro.site) }} />
